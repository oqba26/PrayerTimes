<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PrayerTimes" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Customize your light theme here if needed. -->
    </style>

    <style name="Theme.PrayerTimes" parent="Base.Theme.PrayerTimes" />
</resources>