<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE"
        android:maxSdkVersion="33"
        tools:ignore="ForegroundServicePermission" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" /> -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission
        android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"
        tools:ignore="ProtectedPermissions" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.PrayerTimes"
        tools:targetApi="34">

        <activity
            android:name=".activities.QiblaActivity"
            android:exported="false"
            android:theme="@style/Theme.PrayerTimes" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PrayerTimes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Alarm Activities -->
        <activity
            android:name=".activities.AlarmActivity"
            android:exported="false"
            android:label="@string/title_activity_alarms"
            android:theme="@style/Theme.PrayerTimes" />
        <activity
            android:name=".activities.AlarmRingingActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:showOnLockScreen="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.PrayerTimes.NoActionBar" />

        <!-- Note Activity -->
        <activity
            android:name=".activities.NoteEditorActivity"
            android:label="@string/note_editor_title"
            android:exported="false"
            android:theme="@style/Theme.PrayerTimes.EdgeToEdge" />


        <!-- Services -->
        <service
            android:name=".services.PrayerForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <service
            android:name=".services.AdhanPlayerService"
            android:exported="false"
            android_foregroundServiceType="mediaPlayback" />

        <!-- Receivers -->
        <receiver
            android:name=".services.MidnightReceiver"
            android:enabled="true"
            android:exported="false" />
        
        <receiver
            android:name=".receivers.AdhanAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".receivers.SilentModeReceiver"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name=".receivers.NoteAlarmReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.oqba26.prayertimes.ACTION_SHOW_NOTE_REMINDER" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receivers.AlarmReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.oqba26.prayertimes.ACTION_TRIGGER_ALARM" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receivers.BootAndTimeChangeReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.DATE_CHANGED" />
                <action android:name="android.intent.action.LOCALE_CHANGED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Widgets -->
        <receiver
            android:name=".widget.ModernWidgetProvider"
            android:exported="true"
            android:enabled="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="android.appwidget.action.APPWIDGET_ENABLED" />
                <action android:name="android.appwidget.action.APPWIDGET_DISABLED" />
                <action android:name="ACTION_THEME_CHANGED" />
                <!-- اضافه شده: تا ویجت مستقیم از تغییر تاریخ باخبر بشه -->
                <action android:name="android.intent.action.DATE_CHANGED" />
                <!-- این TIME_TICK در عمل توسط سیستم به Manifest-Receiver داده نمیشه،
                     ولی ضرری هم نداره؛ می‌تونی بذاری بمونه یا حذفش کنی -->
                <action android:name="android.intent.action.TIME_TICK" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/modern_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.LargeModernWidgetProvider"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="ACTION_THEME_CHANGED" />
                <!-- اضافه شده: تا ویجت بزرگ هم مستقیم تغییر تاریخ رو بگیره -->
                <action android:name="android.intent.action.DATE_CHANGED" />
                <action android:name="android.intent.action.TIME_TICK" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/modern_widget_info_large" />
        </receiver>

        <receiver
            android:name=".receivers.IqamaAlarmReceiver"
            android:enabled="true"
            android_exported="false" />

    </application>
</manifest>